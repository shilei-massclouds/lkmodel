[package]
name = "ostd-pod-derive"
version = "0.1.1"
edition = "2021"
description = "The derive proc macro crate for pod-rs"
license = "MPL-2.0"
repository = "https://github.com/asterinas/pod"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "1.0.90", features = ["extra-traits"] }
